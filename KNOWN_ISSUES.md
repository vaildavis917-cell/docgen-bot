# Известные проблемы (Known Issues)

## 🔴 Критические

### 1. Ошибка 409 Conflict и задвоение сообщений

**Описание:**
При определённых условиях бот может отправлять дублирующиеся сообщения. Это связано с ошибкой `409 Conflict: terminated by other getUpdates request` от Telegram API.

**Симптомы:**
- Пользователь получает одно и то же сообщение дважды
- В логах появляется ошибка `409 Conflict`
- Бот может "зависать" на несколько секунд

**Возможные причины:**
1. Запущено несколько экземпляров бота с одним токеном
2. Предыдущий процесс бота не был корректно завершён
3. Кэширование сессий Telegram API
4. Race condition при обработке callback_query

**Временное решение:**
```bash
# Убедитесь что запущен только один экземпляр
pkill -f "python.*bot.py"
sleep 5
python3 bot.py
```

**Как воспроизвести:**
1. Запустить бота
2. Быстро нажимать на кнопки меню
3. Иногда сообщения дублируются

**Нужна помощь:**
Если вы знаете как исправить эту проблему, пожалуйста создайте Pull Request! Возможные направления исследования:
- Добавить уникальный идентификатор для каждого сообщения
- Реализовать дедупликацию на уровне callback_query_id
- Использовать блокировки (locks) для предотвращения race condition

---

## 🟡 Средние

### 2. Медленная обработка больших видео

**Описание:**
Видео размером более 50MB могут обрабатываться очень долго (5+ минут).

**Решение:**
Это ограничение FFmpeg и Telegram API. Рекомендуется использовать видео до 20MB.

---

## 🟢 Незначительные

### 3. Локализация не полная

**Описание:**
Некоторые сообщения отображаются только на русском языке.

**Статус:**
В процессе исправления. Приветствуются PR с переводами.

---

## Как сообщить о новой проблеме

1. Проверьте, что проблема не описана выше
2. Создайте Issue на GitHub с описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Логи (если есть)
   - Версия Python и ОС

---

## Контрибьютинг

Pull Requests для исправления известных проблем приветствуются!

1. Fork репозитория
2. Создайте ветку: `git checkout -b fix/issue-name`
3. Внесите изменения
4. Протестируйте
5. Создайте PR с описанием исправления
