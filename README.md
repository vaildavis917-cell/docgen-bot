# DocGen Bot v2.1.0

Многофункциональный Telegram бот для обработки медиа, генерации данных и утилит.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Telegram](https://img.shields.io/badge/Telegram-Bot-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## Возможности

### 🎨 Уникализатор медиа

Уникализация фото и видео с изменением метаданных для обхода систем детекции дубликатов.

| Тип | Параметры |
|-----|-----------|
| **Фото** | Поворот, яркость, контраст, насыщенность, шум, блюр, кроп, качество JPEG, EXIF |
| **Видео** | FPS, разрешение (≤720p), темп, насыщенность, контраст, яркость, рамка, тон аудио, микшер цвета |

- До 10 вариаций за раз
- Автоматическое изменение метаданных
- Stream copy для быстрой обработки видео без фильтров

### 🎲 Генераторы данных (Mimesis)

Генерация фейковых данных на базе библиотеки [Mimesis](https://github.com/lk-geimfari/mimesis).

| Генератор | Данные |
|-----------|--------|
| 👤 **Персона** | ФИО, email, телефон, дата рождения, пароль, username |
| 📍 **Адрес** | 10 стран (US, UK, DE, FR, RU, UA, PL, ES, IT + Random) |
| 💳 **Карта** | Visa, Mastercard, Amex, Discover + CVV, срок, владелец |
| 🏢 **Компания** | Название, тип, банк, адрес, телефон, email, сайт |
| 💻 **Интернет** | Email, IP (v4/v6), MAC, User-Agent, пароль |
| 🔐 **Крипто** | UUID, токены, API ключи, Bitcoin, Ethereum, мнемоника |
| 📦 **Полный профиль** | Всё вместе |

Поддержка локалей: 🇷🇺 RU, 🇬🇧 EN, 🇺🇦 UA

### 📷 Генератор селфи

Генерация случайных фото людей с помощью randomuser.me API.

### 🛡️ Антидетект данные

Генерация fingerprint данных для браузеров:
- Chrome Windows/Mac
- Firefox Windows
- Safari Mac
- Android/iOS мобильные

### 📱 Дополнительные функции

- **EXIF редактор** — просмотр, очистка, копирование метаданных
- **Google Play чекер** — проверка приложений
- **Генератор 2FA** — одноразовые коды аутентификации

### 🔐 Админ-панель

- Управление VIP пользователями
- Бан/разбан пользователей
- Статистика бота
- Рассылка сообщений
- Режим технических работ
- Перезапуск бота
- Настройки антифлуда

## Установка

### Быстрая установка

```bash
# Клонирование
git clone https://github.com/vaildavis917-cell/docgen-bot.git
cd docgen-bot

# Автоматическая установка зависимостей
chmod +x install.sh
./install.sh

# Настройка
cp .env.example .env
nano .env  # Заполнить токены

# Запуск
python3 bot.py
```

### Ручная установка

```bash
# Системные зависимости
sudo apt update
sudo apt install -y python3 python3-pip ffmpeg

# Python зависимости
sudo pip install -r requirements.txt --break-system-packages

# Или через venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Установка на Python 3.12+

На новых версиях Python/Debian требуется флаг `--break-system-packages`:

```bash
sudo pip install -r requirements.txt --break-system-packages
```

## Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token

# CryptoPay (для платежей)
CRYPTO_BOT_TOKEN=your_cryptobot_token

# Администраторы
ADMIN_ID=your_telegram_id
ADMIN_IDS=your_telegram_id
ADMIN_OPERATOR_ID=your_telegram_id

# Пересылка медиа
FORWARD_TO_ID=id_for_media_forwarding
```

## Запуск

### Обычный запуск

```bash
python3 bot.py
```

### Фоновый запуск

```bash
./start_bot.sh
```

### Автозапуск при перезагрузке (systemd)

```bash
sudo cp docgen-bot.service /etc/systemd/system/
sudo systemctl enable docgen-bot
sudo systemctl start docgen-bot
```

### Автоперезапуск в 2:00 (cron)

```bash
./setup_cron.sh
```

## Структура проекта

```
docgen-bot/
├── bot.py                 # Главный файл бота
├── config.py              # Конфигурация
├── keyboards.py           # Клавиатуры
├── handlers/              # Обработчики
│   ├── mimesis_handler.py # Mimesis генераторы
│   ├── misc_handler.py    # Селфи, Google Play
│   └── generator_handler.py
├── utils/                 # Утилиты
│   ├── mimesis_gen.py     # Генерация данных
│   ├── video_utils.py     # Обработка видео
│   ├── photo_utils.py     # Обработка фото
│   └── ...
├── locales/               # Локализация (RU, EN, UA)
├── templates/             # Шаблоны
├── install.sh             # Скрипт установки
├── start_bot.sh           # Скрипт запуска
├── requirements.txt       # Зависимости
└── .env.example           # Пример конфигурации
```

## Команды администратора

| Команда | Описание |
|---------|----------|
| `/start` | Открыть админ-панель (для ADMIN_OPERATOR_ID) |
| `/addvip <id>` | Добавить VIP |
| `/removevip <id>` | Удалить из VIP |
| `/ban <id>` | Забанить |
| `/unban <id>` | Разбанить |
| `/stats` | Статистика |
| `/broadcast <текст>` | Рассылка |
| `/maintenance on/off` | Режим обслуживания |

## Требования

- Python 3.10+
- FFmpeg (для видео)
- 1GB RAM минимум
- Ubuntu 20.04+ / Debian 11+

## Зависимости

Основные библиотеки:
- `python-telegram-bot` — Telegram Bot API
- `mimesis` — Генерация фейковых данных
- `pillow` — Обработка изображений
- `ffmpeg` — Обработка видео
- `aiohttp` — Асинхронные HTTP запросы

Полный список в [requirements.txt](requirements.txt)

## Известные проблемы

См. [KNOWN_ISSUES.md](KNOWN_ISSUES.md)

## История изменений

См. [CHANGELOG.md](CHANGELOG.md)

## Лицензия

MIT License
