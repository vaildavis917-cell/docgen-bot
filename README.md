# 🤖 DocGen Bot v2.0.0

Многофункциональный Telegram бот для уникализации фото/видео, генерации документов и различных инструментов. Оптимизирован для работы с 300+ пользователями.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Telegram](https://img.shields.io/badge/Telegram-Bot-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## ✨ Возможности

### 📸 Уникализатор фото/видео
- Уникализация изображений (изменение EXIF, хэша, метаданных)
- Уникализация видео (изменение метаданных, битрейта)
- Массовая обработка до 10 файлов
- Поддержка RAR/ZIP архивов

### 🛠️ Инструменты
- **Изменение EXIF** - редактирование метаданных фото
- **Скачивание сайтов** - загрузка веб-страниц
- **Скачивание TikTok** - загрузка видео без водяного знака
- **Уникализатор текста** - изменение текста с сохранением смысла
- **2FA генератор** - генерация кодов двухфакторной аутентификации

### 📄 Генераторы
- Генератор адресов (RU, UA, EN, PL)
- Генератор банковских карт
- Генератор Antidetect профилей
- Генератор селфи с документами

### 💎 Подписки
- Free, Basic ($15), Professional ($20), Premium ($30), Lifetime ($200)
- Интеграция с CryptoPay для оплаты
- VIP система для пользователей

### 🔐 Админ-панель (v2.0)
- Управление VIP пользователями
- Бан/разбан пользователей
- Статистика использования
- Рассылка сообщений
- Режим технических работ (Maintenance)
- **🆕 Настройки антифлуда**
- **🆕 Информация о пользователях**

### 🔒 Безопасность (v2.0)
- **AntiFlood** - автоматическая блокировка при флуде
- **Rate Limiting** - ограничение запросов (30/мин)
- **Input Validation** - проверка входных данных
- **Security Logging** - логирование подозрительной активности
- **Функция "Сообщить об ошибке"** - обратная связь

## 🚀 Установка

### Требования
- Python 3.10+
- Linux (Ubuntu/Debian/Linux Mint)
- FFmpeg (для обработки видео)

### Быстрая установка

```bash
# Клонировать репозиторий
git clone https://github.com/vaildavis917-cell/docgen-bot.git
cd docgen-bot

# Запустить установщик
chmod +x start_bot.sh run_bot.sh
pip install -r requirements.txt

# Настроить .env файл
cp .env.example .env
nano .env

# Запустить бота
./start_bot.sh
```

### Ручная установка

```bash
# Установить зависимости системы
sudo apt update
sudo apt install -y python3 python3-pip python3-venv ffmpeg

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить Python зависимости (включая HTTP/2)
pip install -r requirements.txt

# Запустить бота
python3 bot.py
```

## ⚙️ Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram Bot Token (от @BotFather)
BOT_TOKEN=your_bot_token_here

# CryptoBot Token (для оплаты подписок)
CRYPTO_BOT_TOKEN=your_crypto_bot_token

# ID администратора (полный доступ к админ-панели)
ADMIN_OPERATOR_ID=your_telegram_id

# ID для пересылки медиа (все фото/видео пересылаются сюда)
FORWARD_TO_ID=your_telegram_id

# Список админов через запятую
ADMIN_IDS=your_telegram_id
```

## 🔧 Автозапуск (systemd)

```bash
# Скопировать сервис
sudo cp docgen-bot.service /etc/systemd/system/

# Отредактировать пути в сервисе (замените YOUR_USERNAME)
sudo nano /etc/systemd/system/docgen-bot.service

# Включить и запустить
sudo systemctl daemon-reload
sudo systemctl enable docgen-bot
sudo systemctl start docgen-bot

# Проверить статус
sudo systemctl status docgen-bot

# Посмотреть логи
sudo journalctl -u docgen-bot -f
```

## 📊 Оптимизации для 300+ пользователей

| Компонент | Описание |
|-----------|----------|
| **HTTP/2** | Улучшенная производительность сети |
| **Async Processing** | Видео и фото обрабатываются асинхронно |
| **Connection Pooling** | 100 макс соединений, 50 keep-alive |
| **Rate Limiting** | 30 запросов/минуту на пользователя |
| **AntiFlood** | Защита от спама с настраиваемыми параметрами |

## 👑 Админ-команды

| Команда | Описание |
|---------|----------|
| `/admin` | Справка по командам |
| `/addvip <id> [примечание]` | Добавить VIP |
| `/removevip <id>` | Удалить из VIP |
| `/listvip` | Список VIP |
| `/ban <id> [причина]` | Забанить |
| `/unban <id>` | Разбанить |
| `/banlist` | Список забаненных |
| `/stats` | Статистика |
| `/broadcast <текст>` | Рассылка |
| `/maintenance on/off` | Режим обслуживания |
| `/userinfo <id>` | Инфо о пользователе |

## 📁 Структура проекта

```
docgen-bot/
├── bot.py                 # Главный файл бота
├── config.py              # Конфигурация
├── keyboards.py           # Клавиатуры и кнопки
├── requirements.txt       # Python зависимости
├── start_bot.sh           # Скрипт запуска
├── handlers/              # Обработчики команд
├── utils/                 # Утилиты
│   ├── security.py        # Модуль безопасности
│   ├── performance.py     # Оптимизации
│   ├── video_utils.py     # Обработка видео
│   ├── image_utils.py     # Обработка изображений
│   └── ...
├── locales/               # Локализация (RU, EN, UA)
├── templates/             # Шаблоны документов
└── data/                  # Данные пользователей
```

## 🐛 Известные проблемы

См. [KNOWN_ISSUES.md](KNOWN_ISSUES.md) для списка известных багов и способов их решения.

**⚠️ Главный баг:** При работе двух экземпляров бота возникает 409 Conflict и задвоение сообщений. Убедитесь что запущен только один экземпляр!

## 📝 История изменений

См. [CHANGELOG.md](CHANGELOG.md)

## 🤝 Контрибьютинг

Pull requests приветствуются! Особенно для исправления известных багов.

1. Fork репозитория
2. Создайте ветку (`git checkout -b feature/fix-bug`)
3. Commit изменения (`git commit -m 'Fix: описание'`)
4. Push в ветку (`git push origin feature/fix-bug`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License

---

**⚠️ Важно:** Запускайте только один экземпляр бота! Два одновременно работающих бота вызовут ошибку 409 Conflict.
